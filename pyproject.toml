[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Translate natural language into function calls..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pydantic>=2.13.4",
    "torch>=2.11.0",
    "transformers>=5.8.0",
]
